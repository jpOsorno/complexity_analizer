InsertionSort(A[], n)
begin
    for i ← 2 to n do
    begin
        key ← A[i]
        j ← i - 1
        
        while (j > 0 and A[j] > key) do
        begin
            A[j+1] ← A[j]
            j ← j - 1
        end
        
        A[j+1] ← key
    end
end